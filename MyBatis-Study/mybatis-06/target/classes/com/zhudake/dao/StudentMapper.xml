<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zhudake.dao.StudentMapper">

    <!--方式一，按照查询嵌套处理-->
    <resultMap id="map" type="student">
        <result property="id" column="id"/>
        <result property="name" column="name"/>
        <!--
        复杂属性，需要单独处理
        对象：association
        集合：collection
        -->
        <association property="teacher" column="tid" javaType="teacher" select="getAllTeacher" />
    </resultMap>
    <!--查询学生的全部信息-->
    <select id="getStudent" resultMap="map">
        select * from mybatis.student;
    </select>
    <!--查询老师的全部信息-->
    <select id="getAllTeacher" resultType="teacher">
        select * from mybatis.teacher where id = #{tid}
    </select>

<!--========================================================-->
    <!--方式二，按照结果嵌套处理-->
    <select id="getStudent2" resultMap="map2">
        select s.id sid,s.name sname,t.id tid,t.name tname
        from mybatis.student s,mybatis.teacher t
        where s.tid = t.id;
    </select>

    <resultMap id="map2" type="student">
        <result property="id" column="sid"/>
        <result property="name" column="sname"/>
        <association property="teacher" javaType="teacher">
            <result property="id" column="tid"/>
            <result property="name" column="tname"/>
        </association>
    </resultMap>
</mapper>
